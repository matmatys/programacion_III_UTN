# Foro - Clase 20

## Tema
n8n automation con respuesta AI mock.

## Consigna sugerida
Levantar el stack de clase 20 con Docker Compose, importar el workflow de n8n y responder:

1. Por que agregamos `AiService` en el backend antes de llamar a n8n?
2. Que datos envia el backend al webhook?
3. Que forma tiene la respuesta que espera el backend?
4. Por que el backend local usa `localhost:5690`?
5. Por que el backend Docker usa `chat-n8n:5678`?
6. Por que el frontend no llama directamente a n8n?
7. Que parte queda preparada para conectar AI real en la proxima clase?

## Entrega esperada
- Captura o texto de `docker compose ps`.
- Captura de Docker Desktop mostrando `chat-core-service`, `chat-frontend` y `chat-n8n`.
- Captura del workflow importado y activo en n8n.
- Mensaje enviado desde UTNito con respuesta mock del asistente.
- Respuestas breves a las preguntas.
