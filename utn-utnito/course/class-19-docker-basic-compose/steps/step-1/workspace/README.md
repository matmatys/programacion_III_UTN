# Class 19 workspace - step 1

Accumulated workspace after step 1.

Includes:
- Backend Dockerfile.
- Backend `.env.docker`.
- Backend `.dockerignore`.
- SQLite ready to persist through `/app/database`.

Frontend is still the non-Docker class 16 frontend.
