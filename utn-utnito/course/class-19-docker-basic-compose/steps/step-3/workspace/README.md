# Class 19 workspace - step 3

Final accumulated workspace.

Includes:
- Dockerized backend.
- Dockerized frontend.
- Docker Compose stack in `chat-docker/docker-compose.yml`.
- SQLite persistence through Docker volume.

Run:
```bash
cd chat-docker
docker compose up -d
```
