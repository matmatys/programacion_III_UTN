# Class 19 backend start

Backend base for class 19.

It starts from the class 17 end backend and is intentionally not dockerized yet.

## Run locally

```bash
cp .env.example .env
npm install
npm run start:dev
```

Swagger: `http://localhost:5001/api`

Health: `http://localhost:5001/health`
